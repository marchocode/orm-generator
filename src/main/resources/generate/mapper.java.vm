package ${currentPackage};

import ${JavaEntityImport};

/**
 * ${table.name}
 *
 * @author <a href='https://github.com/marcho-code/orm-generator.git'>orm-generator</>
 * @since ${date}
 */
public interface ${currentClass} {

    int insert(${JavaEntity} entity);

    int updateByPrimaryKey(${JavaEntity} entity);

    ${JavaEntity} selectByPrimaryKey(Integer primaryKey);

    int deleteByPrimaryKey(Integer primaryKey);
}